# Example MCP Definition

> 狀態：Template Example

這是一個 template-safe MCP definition 範例，用來示範 `registry/mcp/` 的最小結構。

## Intended Use

- 取代成你的 server name、command 與 args
- 依各 CLI 實際能力決定是 `native-config`、`pointer` 還是其他 delivery mode
