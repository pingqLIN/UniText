# Canonical MCP Notes
- Codex MCP is configured in: %USERPROFILE%\.codex\config.toml ([mcp_servers.*])
- Claude project MCP is mirrored at: C:\Dev\.mcp.json
- Gemini MCP is configured in settings.json (`mcpServers`)
