# Canonical MCP Notes
- Canonical source: `registry/mcp/claude-project-mcp-seed/definition.json`
- Runnable server: `registry/mcp/claude-project-mcp-seed/server.py`
- Codex MCP is configured in: `%USERPROFILE%\.codex\config.toml` (`[mcp_servers.*]`)
- Claude project MCP is written to: `<repo>/.mcp.json`
- Gemini MCP is configured in settings JSON (`mcpServers`) when the operator chooses to wire it
