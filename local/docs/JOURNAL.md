# Journal

## 2026-03-23

- 建立 `.gitignore` 與 `MILESTONES.md`
- 建立 `registry/skills/` 與 `registry/agents/` roots
- adoption 首批 5 個 canonical skills：`frontend-design`、`pdf`、`docx`、`xlsx`、`mcp-builder`
- 新增 `scan-skills.ps1`、`verify-delivery.ps1`、`health-check.ps1`、`batch-adopt-skills.ps1`
- 將 `sync-skills.ps1` 改為指向 `registry/skills` 並支援 `-DryRun`
- 初始化 Git repository
