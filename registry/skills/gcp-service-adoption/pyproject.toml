[project]
name = "gcp-service-adoption-skill"
version = "0.1.0"
description = "Local authoring scaffold for a GCP service adoption and governance skill."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"
