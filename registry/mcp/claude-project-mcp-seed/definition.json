{
  "mcpServers": {
    "unitext-registry": {
      "transport": "stdio",
      "command": "python",
      "args": [
        "registry/mcp/claude-project-mcp-seed/server.py",
        "--root",
        "."
      ],
      "notes": "Project-local read-only UniText registry server. This relative-path seed is safe to track in GitHub templates; bootstrap may replace command and args with machine-specific absolute paths for the current environment."
    }
  }
}
