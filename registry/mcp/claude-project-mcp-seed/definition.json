{
  "mcpServers": {
    "unitext-registry": {
      "transport": "stdio",
      "command": "python",
      "args": [
        "registry/mcp/claude-project-mcp-seed/server.py",
        "--root",
        "."
      ],
      "notes": "Project-local read-only UniText registry server. This relative-path seed is safe to track in GitHub templates; bootstrap should preserve the tracked seed and keep machine-local wiring outside the shared repo file."
    }
  }
}
