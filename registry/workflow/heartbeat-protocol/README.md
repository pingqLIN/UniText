# Heartbeat Protocol Workflow

This directory holds the sanitized shared guidance for heartbeat protocol handoffs.

UniText keeps this resource as workflow/spec guidance only. It does not store live heartbeat data, private session JSON, terminal transcripts, machine-specific path maps, or runtime evidence.
